The Firebase SDK might throw an error if you try to access a property of a document snapshot before the snapshot has fully loaded.  This often happens when you're working with asynchronous operations and expect data to be available immediately.